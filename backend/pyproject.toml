[tool.pytest.ini_options]
# Permite importar el paquete `app` sin instalarlo (tests corren desde backend/).
pythonpath = ["."]
testpaths = ["tests"]

[tool.ruff]
line-length = 100
target-version = "py311"
